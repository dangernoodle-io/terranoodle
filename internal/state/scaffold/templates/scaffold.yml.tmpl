# Generated by terranoodle state scaffold
# Run: terranoodle state scaffold --plan <plan.json> -o config.yaml
# Tip: use --fetch-registry to auto-populate id templates from provider docs

vars: {}

types:
{{- range .ProviderGroups}}
  # Provider: {{.Provider}}
{{- range .Types}}
  {{.ResourceType}}:
{{- if .HasFields}}
    # Available fields: {{.FieldsComment}}
{{- else}}
    # Available fields: (none)
{{- end}}
    id: {{printf "%q" .IDTemplate}}
{{- end}}
{{- end}}
